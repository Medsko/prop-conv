
// pub fn chop(to_chop: &str, on: &str) -> (&str, &str) {
//     if !to_chop.contains(on) {
//         (to_chop, "")
//     }
//     let mut bits = to_chop.splitn(2, on);
//     let first_bit = bits.next().unwrap();
//     let second_bit = bits.next().unwrap();
//     (first_bit, second_bit)
// }

